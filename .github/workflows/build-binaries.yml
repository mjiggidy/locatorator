# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python application

on:
  push:
    tags:
      - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10

permissions:
  contents: write

jobs:
  build-macos:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"
    - name: Install dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip3 install pyinstaller pillow
        if [ -f requirements.txt ]; then pip3 install -r requirements.txt; fi
    - name: Build .app with pyinstaller
      run: |
        pyinstaller Locatorator.spec
    - name: ZIP the new app
      run: |
        cd dist/
        zip -r -9 "locatorator_${{ github.ref_name }}_mac.zip" *.app
 
    - name: Upload to Release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: "dist/locatorator_${{ github.ref_name }}_mac.zip"
        
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Install NSIS
        run: |
          iwr -useb get.scoop.sh -outfile 'install.ps1'
          .\install.ps1 -RunAsAdmin
          scoop update
          scoop bucket add extras
          scoop install nsis
      - name: Print NSIS version
        run: makensis -VERSION
      - name: Print NSIS compile flags
        run: makensis -HDRINFO
        
#    - name: Upload zipped app as artifact
#      uses: actions/upload-artifact@v3
#      with:
#        path: "dist/locatorator_${{ github.ref_name }}_mac.zip"
